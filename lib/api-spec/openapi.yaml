openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: Driver and Delivery App API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: deliveries
    description: Delivery management for Bridge deliverers
  - name: trips
    description: Trip management for drivers
  - name: drivers
    description: Driver profile and stats
  - name: deliverers
    description: Deliverer profile and stats
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      description: Returns server health status
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /deliveries:
    get:
      operationId: listDeliveries
      tags: [deliveries]
      summary: List all deliveries
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum: [pending, in_progress, delivered, cancelled]
        - name: delivererId
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: List of deliveries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Delivery"
    post:
      operationId: createDelivery
      tags: [deliveries]
      summary: Create a new delivery
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateDeliveryBody"
      responses:
        "201":
          description: Delivery created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Delivery"

  /deliveries/pending-dispatch:
    get:
      operationId: getMyPendingDispatch
      tags: [deliveries]
      summary: Get pending dispatch for this deliverer (no delivery ID needed)
      parameters:
        - name: delivererId
          in: query
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Pending dispatch status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PendingDispatch"

  /deliveries/{id}/dispatch:
    post:
      operationId: dispatchDelivery
      tags: [deliveries]
      summary: Dispatch a delivery to the best available deliverer
      description: Assigns the delivery to the best deliverer (highest rated + fastest). After 60s with no response, cascades to all available deliverers.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Dispatch result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DispatchResult"

  /deliveries/{id}/accept:
    post:
      operationId: acceptDelivery
      tags: [deliveries]
      summary: Livreur accepts a dispatched delivery
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AcceptDeliveryBody"
      responses:
        "200":
          description: Accepted delivery
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Delivery"

  /deliveries/{id}/refuse:
    post:
      operationId: refuseDelivery
      tags: [deliveries]
      summary: Livreur refuses a dispatched delivery - triggers immediate cascade
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RefuseDeliveryBody"
      responses:
        "200":
          description: Delivery refused - cascaded to all deliverers
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Delivery"

  /deliveries/{id}/confirm-delivered:
    post:
      operationId: confirmDelivered
      tags: [deliveries]
      summary: Confirm delivery completed - sends WhatsApp proof to admin
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConfirmDeliveredBody"
      responses:
        "200":
          description: Delivery confirmed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Delivery"

  /deliveries/{id}/pending-dispatch:
    get:
      operationId: getPendingDispatch
      tags: [deliveries]
      summary: Check if there is a pending dispatch for this deliverer
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: delivererId
          in: query
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Dispatch status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PendingDispatch"

  /deliveries/{id}:
    get:
      operationId: getDelivery
      tags: [deliveries]
      summary: Get a delivery by ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Delivery
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Delivery"
    patch:
      operationId: updateDelivery
      tags: [deliveries]
      summary: Update delivery status
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateDeliveryBody"
      responses:
        "200":
          description: Updated delivery
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Delivery"

  /deliveries/stats:
    get:
      operationId: getDeliveryStats
      tags: [deliveries]
      summary: Get delivery statistics summary
      parameters:
        - name: delivererId
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Delivery stats
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeliveryStats"

  /deliverers:
    get:
      operationId: listDeliverers
      tags: [deliverers]
      summary: List deliverers
      responses:
        "200":
          description: List of deliverers
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Deliverer"
    post:
      operationId: createDeliverer
      tags: [deliverers]
      summary: Create a deliverer profile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateDelivererBody"
      responses:
        "201":
          description: Deliverer created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Deliverer"

  /deliverers/{id}:
    get:
      operationId: getDeliverer
      tags: [deliverers]
      summary: Get a deliverer
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Deliverer
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Deliverer"
    patch:
      operationId: updateDeliverer
      tags: [deliverers]
      summary: Update deliverer availability
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateDelivererBody"
      responses:
        "200":
          description: Updated deliverer
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Deliverer"

  /trips:
    get:
      operationId: listTrips
      tags: [trips]
      summary: List all trips
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum: [scheduled, in_progress, completed, cancelled]
        - name: driverId
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: List of trips
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Trip"
    post:
      operationId: createTrip
      tags: [trips]
      summary: Create a new trip
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateTripBody"
      responses:
        "201":
          description: Trip created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Trip"

  /trips/pending-dispatch:
    get:
      operationId: getMyPendingRide
      tags: [trips]
      summary: Get pending ride dispatch for a driver
      parameters:
        - name: driverId
          in: query
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Pending ride or empty
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PendingRideDispatch"

  /trips/{id}/dispatch:
    post:
      operationId: dispatchRide
      tags: [trips]
      summary: Broadcast trip to all available drivers
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Trip dispatched
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Trip"

  /trips/{id}/accept:
    post:
      operationId: acceptRide
      tags: [trips]
      summary: Driver accepts a trip
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AcceptRideBody"
      responses:
        "200":
          description: Trip accepted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Trip"

  /trips/{id}/refuse:
    post:
      operationId: refuseRide
      tags: [trips]
      summary: Driver refuses a trip
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RefuseRideBody"
      responses:
        "200":
          description: Trip refused
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Trip"

  /trips/{id}/pickup-passenger:
    post:
      operationId: pickupPassenger
      tags: [trips]
      summary: Driver confirms passenger is in the car
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PickupPassengerBody"
      responses:
        "200":
          description: Passenger picked up
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Trip"

  /trips/{id}:
    get:
      operationId: getTrip
      tags: [trips]
      summary: Get a trip by ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Trip
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Trip"
    patch:
      operationId: updateTrip
      tags: [trips]
      summary: Update trip status
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTripBody"
      responses:
        "200":
          description: Updated trip
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Trip"

  /trips/stats:
    get:
      operationId: getTripStats
      tags: [trips]
      summary: Get trip statistics for a driver
      parameters:
        - name: driverId
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Trip stats
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TripStats"

  /drivers:
    get:
      operationId: listDrivers
      tags: [drivers]
      summary: List drivers
      responses:
        "200":
          description: List of drivers
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Driver"
    post:
      operationId: createDriver
      tags: [drivers]
      summary: Create a driver profile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateDriverBody"
      responses:
        "201":
          description: Driver created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Driver"

  /drivers/{id}:
    get:
      operationId: getDriver
      tags: [drivers]
      summary: Get a driver
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Driver
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Driver"
    patch:
      operationId: updateDriver
      tags: [drivers]
      summary: Update driver availability or status
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateDriverBody"
      responses:
        "200":
          description: Updated driver
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Driver"

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status

    Delivery:
      type: object
      properties:
        id:
          type: integer
        trackingNumber:
          type: string
        customerName:
          type: string
        customerPhone:
          type: string
        pickupAddress:
          type: string
        deliveryAddress:
          type: string
        status:
          type: string
          enum: [pending, in_progress, delivered, cancelled]
        priority:
          type: string
          enum: [low, normal, urgent]
        weight:
          type: number
        notes:
          type: string
        delivererId:
          type: integer
        estimatedDeliveryTime:
          type: string
        confirmCode:
          type: string
        pickedUpAt:
          type: string
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - trackingNumber
        - customerName
        - pickupAddress
        - deliveryAddress
        - status
        - priority
        - createdAt

    CreateDeliveryBody:
      type: object
      properties:
        trackingNumber:
          type: string
        customerName:
          type: string
        customerPhone:
          type: string
        pickupAddress:
          type: string
        deliveryAddress:
          type: string
        priority:
          type: string
          enum: [low, normal, urgent]
        weight:
          type: number
        notes:
          type: string
        delivererId:
          type: integer
        estimatedDeliveryTime:
          type: string
      required:
        - trackingNumber
        - customerName
        - pickupAddress
        - deliveryAddress
        - priority

    UpdateDeliveryBody:
      type: object
      properties:
        status:
          type: string
          enum: [pending, in_progress, delivered, cancelled]
        notes:
          type: string
        delivererId:
          type: integer

    DeliveryStats:
      type: object
      properties:
        totalToday:
          type: integer
        completedToday:
          type: integer
        inProgress:
          type: integer
        pending:
          type: integer
        cancelled:
          type: integer
        earningsToday:
          type: number
        earningsWeek:
          type: number
        averageDeliveryTime:
          type: number
      required:
        - totalToday
        - completedToday
        - inProgress
        - pending
        - cancelled
        - earningsToday
        - earningsWeek
        - averageDeliveryTime

    Deliverer:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        phone:
          type: string
        email:
          type: string
        vehicleType:
          type: string
          enum: [bicycle, motorcycle, car, van]
        status:
          type: string
          enum: [available, busy, offline]
        zone:
          type: string
        totalDeliveries:
          type: integer
        rating:
          type: number
        photoUrl:
          type: string
        createdAt:
          type: string
      required:
        - id
        - name
        - phone
        - vehicleType
        - status
        - totalDeliveries
        - rating

    CreateDelivererBody:
      type: object
      properties:
        name:
          type: string
        phone:
          type: string
        email:
          type: string
        vehicleType:
          type: string
          enum: [bicycle, motorcycle, car, van]
        zone:
          type: string
      required:
        - name
        - phone
        - vehicleType

    UpdateDelivererBody:
      type: object
      properties:
        status:
          type: string
          enum: [available, busy, offline]
        zone:
          type: string
        photoUrl:
          type: string

    Trip:
      type: object
      properties:
        id:
          type: integer
        passengerName:
          type: string
        passengerPhone:
          type: string
        pickupAddress:
          type: string
        dropoffAddress:
          type: string
        status:
          type: string
          enum: [scheduled, in_progress, completed, cancelled]
        fare:
          type: number
        distance:
          type: number
        duration:
          type: integer
        driverId:
          type: integer
        scheduledAt:
          type: string
        startedAt:
          type: string
        completedAt:
          type: string
        dispatchPhase:
          type: string
        dispatchedAt:
          type: string
        passengerPickedUpAt:
          type: string
        createdAt:
          type: string
      required:
        - id
        - passengerName
        - pickupAddress
        - dropoffAddress
        - status
        - fare
        - createdAt

    PendingRideDispatch:
      type: object
      properties:
        hasPending:
          type: boolean
        trip:
          $ref: "#/components/schemas/Trip"
        expiresAt:
          type: string
        secondsLeft:
          type: integer
        phase:
          type: string
      required:
        - hasPending

    AcceptRideBody:
      type: object
      properties:
        driverId:
          type: integer
      required:
        - driverId

    RefuseRideBody:
      type: object
      properties:
        driverId:
          type: integer
      required:
        - driverId

    CreateTripBody:
      type: object
      properties:
        passengerName:
          type: string
        passengerPhone:
          type: string
        pickupAddress:
          type: string
        dropoffAddress:
          type: string
        fare:
          type: number
        distance:
          type: number
        driverId:
          type: integer
        scheduledAt:
          type: string
      required:
        - passengerName
        - pickupAddress
        - dropoffAddress
        - fare

    UpdateTripBody:
      type: object
      properties:
        status:
          type: string
          enum: [scheduled, in_progress, completed, cancelled]
        startedAt:
          type: string
        completedAt:
          type: string
        fare:
          type: number

    TripStats:
      type: object
      properties:
        totalToday:
          type: integer
        completedToday:
          type: integer
        inProgress:
          type: integer
        cancelled:
          type: integer
        earningsToday:
          type: number
        earningsWeek:
          type: number
        totalKmToday:
          type: number
        averageFare:
          type: number
      required:
        - totalToday
        - completedToday
        - inProgress
        - cancelled
        - earningsToday
        - earningsWeek
        - totalKmToday
        - averageFare

    Driver:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        phone:
          type: string
        email:
          type: string
        licenseNumber:
          type: string
        vehicleModel:
          type: string
        vehiclePlate:
          type: string
        status:
          type: string
          enum: [available, busy, offline]
        totalTrips:
          type: integer
        rating:
          type: number
        photoUrl:
          type: string
        createdAt:
          type: string
      required:
        - id
        - name
        - phone
        - licenseNumber
        - vehicleModel
        - vehiclePlate
        - status
        - totalTrips
        - rating

    CreateDriverBody:
      type: object
      properties:
        name:
          type: string
        phone:
          type: string
        email:
          type: string
        licenseNumber:
          type: string
        vehicleModel:
          type: string
        vehiclePlate:
          type: string
      required:
        - name
        - phone
        - licenseNumber
        - vehicleModel
        - vehiclePlate

    UpdateDriverBody:
      type: object
      properties:
        status:
          type: string
          enum: [available, busy, offline]
        vehicleModel:
          type: string
        vehiclePlate:
          type: string
        photoUrl:
          type: string

    DispatchResult:
      type: object
      properties:
        delivery:
          $ref: "#/components/schemas/Delivery"
        assignedDelivererId:
          type: integer
        assignedDelivererName:
          type: string
        phase:
          type: string
          enum: [primary, cascade]
        message:
          type: string
      required:
        - delivery
        - phase
        - message

    AcceptDeliveryBody:
      type: object
      properties:
        delivererId:
          type: integer
      required:
        - delivererId

    RefuseDeliveryBody:
      type: object
      properties:
        delivererId:
          type: integer
      required:
        - delivererId

    ConfirmDeliveredBody:
      type: object
      properties:
        delivererId:
          type: integer
        confirmCode:
          type: string
        proofNote:
          type: string
      required:
        - delivererId

    PickupPassengerBody:
      type: object
      properties:
        driverId:
          type: integer
      required:
        - driverId

    PendingDispatch:
      type: object
      properties:
        hasPending:
          type: boolean
        delivery:
          $ref: "#/components/schemas/Delivery"
        expiresAt:
          type: string
        secondsLeft:
          type: integer
        phase:
          type: string
      required:
        - hasPending
